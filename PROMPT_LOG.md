# Prompt Log — Лабораторная работа 14

## Task 1 — Разработка сборщика данных на Go
Инструмент: Claude (claude.ai)
Промпт: "Реализовать Go-сборщик данных с Binance WebSocket, горутинами для параллельного сбора по трём парам и записью в JSONL"

Что сделано:
- Создан `collector/main.go` — подключается к Binance WebSocket стриму `@trade` для BTCUSDT, ETHUSDT, BNBUSDT
- Для каждой пары запускается отдельная горутина через `go collectTrades(...)`
- Данные записываются построчно в `data/trades.jsonl`
- Исправлена ошибка парсинга служебных сообщений Binance — поле `e` переведено в `json.RawMessage`, добавлена проверка на пустой `symbol`
- Создан `collector/go.mod` с зависимостью `github.com/gorilla/websocket`

Итого:
- Количество промптов: 2
- Что пришлось исправлять вручную: ничего
- Время: ~20 мин

---

## Task 2+3 — Буферизация, пакетная запись и graceful shutdown
Инструмент: Claude (claude.ai)
Промпт: "Добавить канал с буфером, пакетную запись (batchSize=50, timeout=5s) и graceful shutdown через SIGINT/SIGTERM"

Что сделано:
- Канал `trades` увеличен до буфера на 200 записей
- Реализована функция `batchWriter` — сбрасывает буфер при накоплении 50 записей или каждые 5 секунд по тикеру
- Добавлен `stop`-канал и обработка `syscall.SIGINT` / `syscall.SIGTERM` через `signal.Notify`
- При получении сигнала все горутины-сборщики корректно завершаются, остаток буфера сбрасывается в файл
- Чтение из WebSocket вынесено в отдельную горутину для корректной реакции на `stop`

Итого:
- Количество промптов: 1
- Что пришлось исправлять вручную: ничего
- Время: ~15 мин

---

## Task 4 — Импорт данных в Python (Polars)
Инструмент: Claude (claude.ai)
Промпт: "Написать Python-скрипт для загрузки JSONL в Polars DataFrame с выводом первых 5 строк, типов, количества строк и пропусков"

Что сделано:
- Создан `analysis/task4_import.py` — загружает `data/trades.jsonl` через `pl.read_ndjson()`
- Выводит первые 5 строк, размер датафрейма, типы столбцов, количество пропусков по каждому столбцу, базовую статистику через `df.describe()`
- Создан `analysis/requirements.txt` — polars, duckdb, plotly, kaleido

Итого:
- Количество промптов: 1
- Что пришлось исправлять вручную: ничего
- Время: ~10 мин

---

## Task 5 — Очистка и валидация данных
Инструмент: Claude (claude.ai)
Промпт: "Очистить данные в Polars: удалить дубликаты, привести типы price/quantity к Float64, timestamp и collected_at к Datetime, обработать пропуски"

Что сделано:
- Создан `analysis/task5_clean.py` — 7 задокументированных шагов очистки
- Удалены дубликаты по `trade_id`, `price` и `quantity` приведены к `Float64`
- `timestamp` приведён к `Datetime` из формата `"%Y-%m-%d %H:%M:%S%.3f"`
- `collected_at` приведён к `Datetime`
- Добавлена фильтрация аномалий (`price > 0` и `quantity > 0`), сортировка по времени
- Исправлена ошибка формата timestamp — реальный формат в файле оказался `"2026-05-29 19:09:47.834"`
- Очищенные данные сохранены в `data/trades_clean.jsonl`

Итого:
- Количество промптов: 2
- Что пришлось исправлять вручную: ничего
- Время: ~15 мин

---

## Task 6 — Агрегационный анализ
Инструмент: Claude (claude.ai)
Промпт: "Выполнить группировку по symbol с агрегатами SUM, AVG, MIN, MAX, COUNT, а также по стороне сделки и минутные OHLCV свечи"

Что сделано:
- Создан `analysis/task6_aggregation.py` — три агрегации: по символу, по стороне сделки (buy/sell), OHLCV свечи по минутам
- Исправлена ошибка `truncate` — `timestamp` не был приведён к `Datetime` при загрузке, добавлен явный каст в `load_clean()`
- Исправлен формат парсинга timestamp на `"%Y-%m-%d %H:%M:%S%.3f"`

Итого:
- Количество промптов: 2
- Что пришлось исправлять вручную: ничего
- Время: ~20 мин

---

## Task 7 — Сохранение в Parquet
Инструмент: Claude (claude.ai)
Промпт: "Сохранить очищенный DataFrame в Parquet с компрессией snappy, сравнить размер с JSONL, верифицировать чтением обратно"

Что сделано:
- Создан `analysis/task7_parquet.py` — сохраняет DataFrame через `df.write_parquet()` с `compression="snappy"`
- Выводит сравнение размеров файлов: JSONL 293 KB → Parquet 18.9 KB, сжатие 15.5x
- Верификация: файл читается обратно, проверяется количество строк и типы данных
- Запись заняла 10 мс

Итого:
- Количество промптов: 1
- Что пришлось исправлять вручную: ничего
- Время: ~10 мин

---

## Task 8 — Анализ через DuckDB (SQL)
Инструмент: Claude (claude.ai)
Промпт: "Выполнить SQL-запросы к Parquet через DuckDB с фильтрацией, группировкой и сортировкой, сравнить производительность с Polars"

Что сделано:
- Создан `analysis/task8_duckdb.py` — три SQL-запроса через `duckdb.connect()` и `read_parquet()`
- Те же три агрегации реализованы на Polars для сравнения
- Замерено время выполнения через `time.perf_counter()`
- Результат: Polars быстрее на малых данных (1721 строк), DuckDB показал холодный старт 3414 мс на первом запросе (инициализация движка), последующие запросы 4-5 мс

Итого:
- Количество промптов: 1
- Что пришлось исправлять вручную: ничего
- Время: ~15 мин

---

## Task 9 — Визуализация результатов
Инструмент: Claude (claude.ai)
Промпт: "Построить 3 интерактивных графика через Plotly: минутные свечи, гистограмма распределения цен, сравнение объёмов"

Что сделано:
- Создан `analysis/task9_visualization.py` — три графика сохраняются в `charts/`
- `chart1_candlestick.html` — минутные OHLCV свечи для всех трёх пар на тёмном фоне
- `chart2_distribution.html` — гистограмма распределения цен по парам
- `chart3_volume.html` — bar chart объёма в USD + pie chart доли сделок
- Исправлена ошибка `ModuleNotFoundError: No module named 'pyarrow'` — установлен `pip install pyarrow`

Итого:
- Количество промптов: 1
- Что пришлось исправлять вручную: установка pyarrow
- Время: ~15 мин

---

## Task 10 — Документирование конвейера
Инструмент: Claude (claude.ai)
Промпт: "Подготовить README с архитектурой конвейера, инструкцией по запуску, примерами SQL-запросов и результатами"

Что сделано:
- Обновлён `README.md` — ASCII-схема ETL-конвейера, структура репозитория, инструкция по запуску, примеры DuckDB SQL-запросов, таблицы с результатами агрегации и сравнением производительности

Итого:
- Количество промптов: 1
- Что пришлось исправлять вручную: ничего
- Время: ~10 мин